[package]
name = "pasadafund"
version = "0.1.0"
edition = "2024"

[dependencies]
soroban-sdk = "22.0.7"

[dev-dependencies]
soroban-sdk = { version = "22.0.7", features = ["testutils"] }

[profile.release]
overflow-checks = true
